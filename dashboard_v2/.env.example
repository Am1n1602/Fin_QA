# Copy to .env.local (Vite auto-loads it; gitignored) and adjust if finqa_v2's API
# isn't on the default host/port (see finqa_v2/api/config.py -- default 8010).
VITE_API_BASE_URL=http://localhost:8010
