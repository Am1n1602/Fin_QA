global:
  scrape_interval: 15s

scrape_configs:
  - job_name: finqa-api-v2
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8010"]     # compose service name, not localhost -- Prometheus runs in its own container
